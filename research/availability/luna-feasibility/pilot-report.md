枚方の人手比較も完了：[枚方レビュー](hirakata-review.md)。3回とも10件すべて判断不能で一致したが、日別資料不足のため確定availabilityの増加にはつながらない。

# API試行結果（人手正解との比較前）

2026-09-21追記：岡崎10件の人手判定との比較・元資料再確認は [岡崎レビュー](okazaki-review.md) を参照。原記入との一致は各回7/10、資料からの修正案との一致は各回10/10。引用元の誤りと評価範囲の制限は同レビューに記録。

以下は2026-09-20時点の記録。2026-09-21の府中人手判定との比較は [府中レビュー](fuchu-review.md) を参照。

2026-09-20、OpenAI Responses APIに `gpt-5.6-luna`、`reasoning.effort: none` を指定して実行。
最小接続確認1回と、3施設×3回の読解9回が成功した。全応答でmodelとeffortを確認し、reasoning tokensは0だった。
他モデルへの切り替えやプロンプト調整は行っていない。

## 実行条件

- 各施設10日を1リクエストにまとめ、同じ内容を3回送信。合計90レコード。
- 府中はカレンダー全ページPNGと案内テキスト、岡崎は週間画像・休止PDFの全ページPNG・告知テキスト、枚方は案内テキストを入力。画像はdetail high。
- 人手シートから読み込むのはIDと日付だけ。READMEのケース解説や人手正解は渡していない。
- Structured Outputs、max_output_tokens 6000、store false。外部toolなし。
- 保存元資料の全SHA-256を送信前に検証。回答の全ID・日付の一致、完了状態を検証。
- リクエスト、全応答、解析JSON、集計、スクリプトはresults/に保存。キーは含まない。

## 観測結果

| 指標 | 結果 |
|---|---|
| API成功 | 9/9 |
| JSON解析・ID/日付網羅 | 90/90 |
| 3回ともstatusが一致したケース | 府中8/10、岡崎10/10、枚方10/10 |
| 利用時間の開始・終了が3回とも一致したケース | 府中8/10、岡崎10/10、枚方10/10 |
| periods全体が一致したケース（scope表記も含む厳密比較） | 府中5/10、岡崎10/10、枚方10/10 |
| 入力token合計 | 62,832 |
| 出力token合計 | 12,749 |
| reasoning token合計 | 0 |
| 1リクエストの時間 | 中央値7.48秒、範囲6.12〜9.74秒 |
| 推定API費用 | US$0.02137535（接続確認を除く） |

費用は[公式モデル単価](https://developers.openai.com/api/docs/models/gpt-5.6-luna)の100万tokenあたり入力$0.20、cache write $0.25、cache hit $0.02、出力$1.20をusageへ適用した概算。請求明細ではない。繰り返し実行ではキャッシュが効いているため、別資料を毎回送る場合へ単純外挿しない。

根拠配列が空の出力が10/90件あった。根拠不足をunknownとする場合も含み、直ちに誤答とは扱わないが、根拠不足の説明を含めた人手レビューが必要。
時間形式・開始終了順の簡易チェックでは不正を検出しなかった。根拠の内容が正しいことまでは検証していない。

## 現時点の判断

指定モデル・noneで画像と文章を低費用で構造化できることは確認できた。一方、府中では2ケースのstatusが揺れており、現在のプロンプトのまま自動公開できるとは判断しない。
反復一致は正答率ではない。人手正解が未記入なのでaccuracy・誤った利用可判定率は未算出。
期間の比較にはscopeの文言差も含まれるため、意味の違いと表現差を分離して人手評価する。

次はannotation.mdを独立に記入し、固定した今回の出力と照合する。個別回答は正解記入前に見ないことを推奨する。
その後、開放枠を含む岡崎の別週資料と、日付付き文章の追加資料で評価を広げる。今回のサンプルは不可・unknownに偏っており、全面採用判断には不十分。

公開アプリ・collector・availabilityデータ・workflowは変更なし。API試行と研究記録だけのため、アプリbuild/daily gateは未実行。主担当が送信条件・保存結果・集計を確認し、sub-agentへの委譲は行っていない。
